local wezterm = require 'wezterm';

return {
	color_scheme = "Urple",
    font = wezterm.font("Terminus"),
    font_size = 12,
    use_fancy_tab_bar = false,
    window_padding = {
        left = 8,
        right = 8,
        bottom = 2,
        up = 8,
    }
}
